# -*- conf -*-
# Mirrors ~/.dots/ghostty/.config/ghostty/config: ghostty's
# focus-follows-mouse/confirm-close-surface have no foot equivalent (foot
# has no splits, and no close-confirmation prompt), so this file is just
# the theme include.

include=~/.rice/foot/theme.ini
